const statusMessage = {
    created: 'user account created',
    serverError: 'server error, try again',
    badRequest: 'Your input is invalid',
    forbidden: 'Email already used',
    success: 'user successfully signed in'
}

export default statusMessage;
